name: nblue-rests
base: 'test/scripts'
methods:
  get:
    '/run/:script': run
    '/:script': run
  post:
    '/run/:script': run
    '/:script': run
middlewares:
  - nblue-json
  - nblue-data
  - nblue-scope
prefix: '/rests'
settings:
  jsonError: false
