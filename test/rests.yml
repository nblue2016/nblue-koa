name: nblue-rests
base: 'test/scripts'
routes:
  get:
    '/run/:script': run
    '/:script': run
  post:
    '/run/:script': run
    '/:script': run
middlewares:
  - nblue-json
  - nblue-data
  - nblue-scope
prefix: '/apis'
settings:
  jsonError: false
