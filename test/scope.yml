name: scope

database:
  connection: 'sqlite://test/data/scope.sqlite'

settings:
  ignoreCase: false
  allowGuest: true

middlewares:
  - json
  - scope

routes:
  get:
    '/list': list
    '/ls': list
  post:
    '/create': create

prefix: '/api/scope'
