name: scope

database:
  connection: 'sqlite://test/data/scope.sqlite'

settings:
  ignoreCase: false
  allowGuest: false
  mergeRouter: models

routes: {}

middlewares:
  - json
  - scope

prefix: '/api/scope'
