name: nblue-scope

database:
  connection: 'sqlite://test/data/scope.sqlite'

settings:
  ignoreCase: false

middlewares:
  - nblue-json
  - nblue-scope

routes:
  get:
    '/list': list
  post:
    '/create': create

prefix: '/api/scope'
