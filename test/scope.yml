name: scope

database:
  connection: 'sqlite://test/data/scope.sqlite'

settings:
  ignoreCase: false
  allowGuest: false

middlewares:
  - json
  - scope

routes:
  get:
    '/list': list
    '/ls': list
    '/model': model
  post:
    '/create': create

prefix: '/api/scope'
