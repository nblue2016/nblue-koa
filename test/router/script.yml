name: script
base: 'test/scripts'

routes:
  get:
    '/run/:script': run
    '/:script': run
  post:
    '/run/:script': run
    '/:script': run
  options:
    '/*': pass
  all:
    '/*': disable

middlewares:
  - json
  - scope

prefix: '/scripts'
settings:
  jsonError: false
