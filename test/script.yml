name: script
base: 'test/scripts'

routes:
  get:
    '/run/:script': run
    '/:script': run
  post:
    '/run/:script': run
    '/:script': run

middlewares:
  - json
  - data
  - scope

prefix: '/apis'
settings:
  jsonError: false
