name: script
base: 'test/scripts'

routes:
  get:
    '/run/:script': run
    '/:script': run
  post:
    '/run/:script': run
    '/:script': run

middlewares:
  - json
  - scope

prefix: '/apis'
settings:
  jsonError: false
